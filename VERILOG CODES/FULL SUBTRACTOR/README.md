The Full subtractor is implemented using NOR gates only. 
I have made three different sub-modules which is the basis of the top level module. XOR logic, OR logic and AND logic are implemented using NOR gates only.
